{
  "name": "uswds-dev-extension",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/background.js src/injected-script.jsx --jsx=automatic --bundle --minify --outdir=dist --target=es2020",
    "watch": "npm run build -- --watch",
    "bundle": "./scripts/bundle.sh"
  },
  "author": "Sawyer Hollenshead",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.20",
    "@types/react-dom": "^18.2.7",
    "chrome-types": "^0.1.222",
    "esbuild": "^0.19.1",
    "typescript": "^5.1.6"
  }
}
